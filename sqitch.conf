[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "cards"]
	uri = db:pg:cards
[deploy]
	verify = true
[rebase]
	verify = true
[engine "pg"]
	target = cards
